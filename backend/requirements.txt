# ======================================
# Kovir Cash - Requirements do Backend
# ======================================

# --- Web Framework ---
fastapi==0.109.0           # Framework web assíncrono, rápido e com validação automática integrada
uvicorn==0.27.0            # Servidor ASGI (Application Server) que roda aplicações FastAPI

# --- Banco de Dados ---
sqlalchemy==2.0.23         # ORM (Object-Relational Mapping) para abstrair queries SQL em Python
psycopg[binary]     # Driver PostgreSQL - permite SQLAlchemy comunicar com PostgreSQL
alembic==1.12.1            # Ferramenta de migrations - versionamento e controle do schema do banco

# --- Validação de Dados ---
pydantic==2.6.0            # Validação de dados em tempo de execução (schemas de entrada/saída)
pydantic-settings==2.1.0   # Gerenciador de variáveis de ambiente (.env) com tipagem

# --- Upload de Arquivos ---
python-multipart==0.0.6    # Parse de dados multipart (formulários, uploads)

# --- Autenticação & Segurança ---
python-jose==3.3.0         # Implementação de JWT (JSON Web Tokens) para autenticação
passlib==1.7.4             # Biblioteca para hash de senhas (suporta bcrypt)
bcrypt==4.1.1              # Algoritmo de hash bcrypt para criptografia de senhas

# --- Variáveis de Ambiente ---
python-dotenv==1.0.0       # Carrega variáveis de ambiente do arquivo .env para config

# --- Testes ---
pytest==7.4.3              # Framework de testes unitários
pytest-asyncio==0.21.1     # Plugin pytest para testar código assíncrono (async/await)
httpx==0.25.2              # Cliente HTTP (como requests mas assíncrono) para testes de API